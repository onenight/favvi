<div class="row">
  <div class="span12 offset1">
<% @products.each do |product| %>

	<div class="span3">
    	<hr>
	  	<p>
			<h3><%= product.name %></h3>
	    </p>
	    <hr>
	<p><%= image_tag product.image.url(:thumb) %></p>
    <p>$ <%= product.price %></p>
    <p><%= link_to 'Edit', edit_product_path(product) %> |
    <%= link_to 'Destroy', product, :confirm => 'Are you sure?', :method => :delete %></p>
	</div>
<% end %>
  </div>
</div>

<br />

<%= content_for :sidebar do %>
<%= link_to 'New Style', new_product_path, :class => "btn" %>
<%= render "cell/sidebar_all_brands" %>
<% end %>
